Install feh and pyalsaaudio. Then run this. ::

    pip3 install netwaves

Then run this.

    netwaves.py


Separate the color field from the sound field.
That should make this easier.

And move the python stuff to haskell.
